# Copy to .env and adjust as needed.
# SQLite (default — no setup required):
DATABASE_URL=sqlite:///pl_predictor.db

# PostgreSQL (optional, requires Docker or a running Postgres instance):
# DATABASE_URL=postgresql://pluser:plpass@localhost:5432/pl_predictor
